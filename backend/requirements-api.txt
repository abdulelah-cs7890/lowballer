# Slim runtime deps for the deployed API (Render). The live eBay path is pure
# FastAPI + SQLAlchemy + httpx — no ML/data stack (scikit-learn, xgboost, numpy,
# pandas, joblib) and no Haraj scraper. Those live in the archived legacy/ pipeline
# and the full requirements.txt. Keeping this lean = faster, smaller free-tier builds.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
httpx>=0.27
