"""eBay integration: OAuth client + Browse API search, comps-median valuation.

Unlike Haraj, eBay publishes a price on every listing and exposes exact-model search,
so we can compute a fair value from comparable listings and flag genuine underpricing.
"""
